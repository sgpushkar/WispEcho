# Project Guidelines

## Git & Deployment Rules
- **NEVER execute `git push` or deploy code automatically.**
- Always provide the user with exact git commands or ask for explicit confirmation before running any push command.
